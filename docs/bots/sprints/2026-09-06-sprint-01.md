# Sprint 01 — 2026-09-06

**Goal:** The repository is a Bun workspace where the VS Code extension and a
`bunx github:flying-dice/repodoc` CLI drive one shared core, and agents work the
board through that CLI.

**Done means:** `bun run check-types`, `bun run lint`, and `bun run test` are
green; the extension bundles and packages; the CLI creates, moves, comments on,
and gate-checks cards in a scratch repo; the docs and skill describe the new
layout.

## Tasks

| # | Task | Owner | Status | Evidence |
| --- | --- | --- | --- | --- |
| 1 | Move `src/core` + adapters to `packages/core`, unit tests to `bun test` | lead | shipped | 201 → 212 core/unit tests pass |
| 2 | Move extension to `packages/vscode`, consume `@repodoc/core` | lead | shipped | `bun run compile` bundles; `vsce package --no-dependencies` produces the VSIX |
| 3 | Core API: result-returning mutations, `updateCardMeta`, `recordGateEvidence`, parse `agent:` | lead | shipped | `packages/core/test/store.meta.test.ts` |
| 4 | CLI with board/column/card/decision/docs/skill commands, gate enforcement, `--json` | lead | shipped | `packages/cli/test/cli.test.ts` (27 cases), smoke run in a scratch repo |
| 5 | Docs, ADR 09, skill CLI section, CI on Bun | lead | shipped | this commit |
| 6 | Run the VS Code e2e suite | lead | carried | VS Code download blocked by the sandbox proxy twice (ECONNRESET at 338 MB); CI runs it under xvfb |

## Decisions made

- Decision 09 — one core, two hosts; CLI is dependency-free and imports core by
  relative path so `bunx github:` works without an install step.
- Unit tests moved from mocha-in-extension-host to `bun test`; the e2e suite
  stays on `@vscode/test-cli`.
- Agent team installed with `bots` (vanilla variant, Claude + Codex harnesses,
  project scope). The lead runs sessions; the roster is under `.claude/agents/bots/`.

## Carried

- Task 6 above. Also: the `bots`-managed copy of the `repodoc-workflow` skill
  under `.claude/skills/` predates the CLI section; the canonical text lives in
  `packages/core/src/skillContent.ts` and the extension offers a sync.
