# Sprint 02 — 2026-09-06

**Goal:** Gates feed agents the workflow; the VS Code UI reaches parity with
the CLI and hand-editing; Gherkin feature sets sit on the kanban surface; the
repo adopts Biome, super-strict TypeScript and current dependencies.

**Done means:** `card move` refuses with each gate's `prompt` and prints the
column `prompt` on success (tests); the designer's parity gaps G-1..G-4, G-6,
G-7, decision status and defects D-1/2/4/6/8 are implemented in the webview
with matching store/CLI methods; `feature` CLI group and feature sets render in
the extension (tests); `biome check .` and `tsc` under the strict flag set are
clean; `bun run test` green; CI green including e2e.

## Tasks

| # | Task | Owner | Status | Evidence |
| --- | --- | --- | --- | --- |
| 1 | Gate/column `prompt`; CLI refusal prints prompts; `--override --reason` | lead | shipped | commit 211a62b; cli.test.ts gates suite |
| 2 | UI parity sweep (read-only design doc) | designer | shipped | docs/bots/design/2026-09-06-ui-parity-sweep.md |
| 3 | Feature sets: parser, FeatureStore, CLI `feature` group, BoardSource in the extension, dogfood `features/repodoc` | senior-developer | shipped | 69cd554; 21 tests |
| 4 | Dependency bumps (TS 7, esbuild 0.28, @types/node 26, test-cli/electron, vsce, mermaid); ESLint → Biome config | lead | shipped (config) | `bun run check-types` 3/3, 243 tests |
| 5 | Core + CLI parity additions: `addChecklistItem`, `setDescription`, decision `setStatus`; `card check-add`, `card describe`, `decision status` | developer | shipped | 0ef6e5b |
| 6 | Webview parity: G-1 open-file routes, G-2 `updateMeta`, G-3/G-4 guided blocked dialog with prompts, inline `recordGatePass`, required reason; D-1/2/4/6/8 | senior-developer | shipped | 54a2a42 |
| 7 | Biome format/lint sweep + super-strict tsconfig fixes (~500 diagnostics measured: core 181, cli 165, vscode 151) | senior-developer | shipped | 5e6e6c8: 0 diagnostics from ~190/pkg, Biome clean |
| 8 | e2e suite in CI | lead | carried | proxy blocks the VS Code download locally |
| 9 | Card ids visible on face + modal; "Copy ref" (`<board>/<id> — <title> (<path>)`) in webview, tree menu and `card show` | lead | shipped | ae19d54 |
| 10 | QA: tester-deep adversarial pass over core/CLI + e2e coverage for feature tree, feature panel and capabilities | tester-deep | shipped | 6224668: 180 tests, 8 defects → fixed in b406533 |
| 11 | Clean-code review of the branch diff, `refactor` until no marker > 0.5, change-review re-run; record gate evidence; move card 17 to review | lead | shipped | e16d4b6 (audit fixes); card 17 in review |

## Decisions made

- Decision 10: feature status is an `@status:` tag; gates not enforced for
  feature sets; sidecar deferred.
- marked stays at 15: v18 is ESM-only and the extension host loads CommonJS.
- `@types/vscode` stays at 1.100 to match `engines.vscode`.
- Sequencing: features → core/CLI parity → webview parity → strict/Biome sweep,
  because the last two touch the same files as the first.

## Incident

An accidental `git checkout -- .` by the lead reverted unstaged tracked-file
edits mid-sprint (the lead's dependency bumps, redone; the senior developer
was told to re-verify their tracked edits). Lesson: never discard with a
working-tree-wide checkout while agents are editing; use `git stash` scoped to
paths or a worktree for trial runs.

## Clean-code audit (task 11) — findings above 0.5

Eight parallel reviewers, one per principle, read-only. Everything above 0.5
is being fixed in one pass (senior-developer); nothing is tagged because
nothing above the bar is left standing.

| Principle | Above 0.5 | What |
| --- | --- | --- |
| SRP | 1 | Markdown body editors live in store.ts → `cardBody.ts` |
| DRY | 6 | Three copies of the section-scan; seven mutators repeat find→update→fire; board/feature config projection duplicated; default gate prompt in two hosts; card/feature list twins |
| NAMING | 1 | `describe(error)` beside the `card describe` command |
| COUPLING | 1 | BoardPanel takes the store only for `.root` |
| DEAD | 3 | `MoveBlockedGate.kind/.satisfied`, `nextColumnId`, `BoardsNode.card.agent` |
| KISS | 2 | Throwaway `CardBoardSource` for one lookup; capabilities inferred from optional methods |
| BOUNDARY | 1 | Gate prompt wording owned by both hosts, not core |
| PANIC | 3 | `skill install toString` crash; newline injection into frontmatter via CLI flags / webview labels; duplicate slugs crash the Boards tree |

Also fixed although at or below 0.5: unchecked `gateId` from the webview,
host-composed on-disk paths, `openFeature` bypassing the containment check.

## Outcome

Card 17 moved to **In Review** through the CLI with all three enter gates
recorded from real runs. Open for a human: `comments-addressed`,
`peer-reviewed`, and the e2e result on CI (VS Code cannot be downloaded in
the sandbox). Follow-ups noted by the team, not scheduled: a user-facing
message when the board refuses a move for a store reason (needs
`storeErrorMessage` in core), `verbatimModuleSyntax` for the extension
package (requires ESM), a webview/protocol drift check for `media/board.js`.
