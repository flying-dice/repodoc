{
  "name": "@repodoc/cli",
  "version": "0.9.0",
  "private": true,
  "description": "RepoDoc command line — work boards, cards, decisions and docs from a terminal or an agent.",
  "license": "MIT",
  "bin": {
    "repodoc": "./src/bin.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "typescript": "^7.0.2"
  }
}
