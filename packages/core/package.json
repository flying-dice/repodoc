{
  "name": "@repodoc/core",
  "version": "0.9.0",
  "private": true,
  "description": "Host-agnostic RepoDoc domain: boards, cards, gates, decisions, docs — over a filesystem port.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "typescript": "^7.0.2"
  }
}
